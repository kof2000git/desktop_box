<Window x:Class="DesktopBox.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DesktopBox.ViewModels"
        xmlns:ctl="clr-namespace:DesktopBox.Controls"
        Title="DesktopBox"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ShowActivated="False"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        UseLayoutRounding="True">
    <Canvas x:Name="Layer" Background="Transparent" ClipToBounds="False">
        <Canvas.ContextMenu>
            <ContextMenu>
                <MenuItem Header="新建盒子" Click="OnNewBox"/>
                <MenuItem Header="一键整理桌面" Click="OnOrganize" FontWeight="SemiBold"/>
                <MenuItem Header="还原整理" Click="OnRestoreOrganize"/>
                <MenuItem Header="设置" Click="OnOpenSettings"/>
                <Separator/>
                <MenuItem Header="退出" Click="OnQuit"/>
            </ContextMenu>
        </Canvas.ContextMenu>
        <ItemsControl ItemsSource="{Binding Boxes}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding X}"/>
                    <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:BoxViewModel}">
                    <ctl:BoxControl/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Canvas>
</Window>
